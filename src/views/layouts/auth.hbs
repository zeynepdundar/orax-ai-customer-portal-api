{{!--
  Auth layout — used by /login. No app shell (no sidebar/header).
  Equivalent to the (auth) route group in the Next.js portal.
--}}
<!DOCTYPE html>
<html lang="{{locale}}" class="h-full antialiased">
<head>
  {{> head}}
</head>
<body class="min-h-full">
  {{{body}}}
</body>
</html>
